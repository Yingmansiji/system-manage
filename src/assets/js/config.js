var config = {
  /*--获取图片接口--*/
  // 本地调试
  // imgBaseUrl: "/api",
  // 打包至测试及正式
  imgBaseUrl: window.location.origin + "",

  // 配置文字
  shareUrl: window.location.origin + "",
};

export default {
  config,
};
